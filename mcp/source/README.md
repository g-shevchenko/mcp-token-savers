# HWAI MCP Bundled Source

This directory is the self-contained MCP source tree used by `../install.sh`.
It is intentionally limited to MCP services and helper scripts required for
build, stdio startup, doctor, and proof loops.

Do not commit local caches, request logs, feedback logs, screenshots, traces,
env files, credentials, Notion exports, or raw customer artifacts here.
